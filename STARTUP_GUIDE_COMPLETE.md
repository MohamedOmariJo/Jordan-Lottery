# 🚀 دليل التشغيل الشامل للمبتدئين

## ⚡ البدء السريع (5 دقائق)

### الخطوة 1: تحضير البيئة

```bash
# 1. تأكد من Python (يجب أن يكون 3.8 أو أحدث)
python --version

# 2. إنشاء مجلد للمشروع
mkdir jordan_lottery
cd jordan_lottery

# 3. وضع جميع الملفات في هذا المجلد
# - app_enhanced_fixed.py (أو app_enhanced.py)
# - 249.xlsx
# - requirements_fixed.txt
# - run_app.py
# - diagnostic_test.py
```

### الخطوة 2: تثبيت المكتبات

```bash
# الطريقة الموصى بها (مع بيئة افتراضية)
python -m venv venv

# تفعيل البيئة
# على Windows:
venv\Scripts\activate

# على Mac/Linux:
source venv/bin/activate

# تثبيت المكتبات
pip install -r requirements_fixed.txt
```

**أو الطريقة المباشرة (بدون بيئة افتراضية):**

```bash
pip install streamlit pandas numpy plotly openpyxl reportlab scikit-learn psutil
```

### الخطوة 3: فحص الجاهزية

```bash
# تشغيل الفحص التلقائي
python diagnostic_test.py --quick
```

**يجب أن ترى:**
```
✅ Python Version: نجح
✅ Libraries: نجح
✅ File Permissions: نجح
```

### الخطوة 4: تشغيل التطبيق

```bash
# الطريقة 1: باستخدام السكريبت الذكي (موصى به)
python run_app.py --debug

# الطريقة 2: تشغيل مباشر
streamlit run app_enhanced_fixed.py

# الطريقة 3: على منفذ مختلف
streamlit run app_enhanced_fixed.py --server.port 8502
```

### الخطوة 5: استخدام التطبيق

1. **سيفتح المتصفح تلقائياً** على http://localhost:8501
2. **في الشريط الجانبي الأيسر:**
   - اضغط على "رفع ملف"
   - اختر ملف `249.xlsx`
   - انتظر رسالة "✅ تم تحميل 249 سحب"
3. **ابدأ الاستكشاف:**
   - جرب تبويب "Dashboard"
   - ثم "المولد الذكي"
   - جرب توليد 3 تذاكر

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: خطأ "ModuleNotFoundError"

```bash
# السبب: مكتبة غير مثبتة
# الحل:
pip install [اسم المكتبة]

# مثال:
pip install streamlit
```

### المشكلة 2: "Port 8501 is already in use"

```bash
# الحل 1: استخدام منفذ مختلف
streamlit run app_enhanced_fixed.py --server.port 8502

# الحل 2: إيقاف العملية القديمة
# Windows:
taskkill /F /IM streamlit.exe

# Mac/Linux:
killall streamlit
```

### المشكلة 3: خطأ في قراءة ملف Excel

```bash
# تحقق من الملف
python -c "import pandas as pd; df = pd.read_excel('249.xlsx'); print(df.head())"

# إذا فشل، جرب توليد بيانات تجريبية:
python diagnostic_test.py --generate-data
```

### المشكلة 4: التطبيق بطيء أو يتوقف

**في واجهة التطبيق:**
1. افتح الشريط الجانبي
2. ابحث عن "🔧 أدوات التصحيح"
3. اضغط "🗑️ مسح الذاكرة المؤقتة"

### المشكلة 5: لا يظهر شيء في المتصفح

```bash
# افتح المتصفح يدوياً وانتقل إلى:
http://localhost:8501

# أو:
http://127.0.0.1:8501
```

---

## 📂 هيكل الملفات الصحيح

```
مشروع_اليانصيب/
│
├── 📄 app_enhanced_fixed.py      # التطبيق الرئيسي (محسّن)
├── 📄 app_enhanced.py            # التطبيق الأصلي (نسخة احتياطية)
├── 📄 requirements_fixed.txt     # المكتبات المحدّثة
├── 📄 run_app.py                 # سكريبت التشغيل الذكي
├── 📄 diagnostic_test.py         # أداة التشخيص
│
├── 📁 data/                      # مجلد البيانات
│   ├── 249.xlsx                 # البيانات الأصلية
│   └── sample_data_generated.xlsx  # بيانات تجريبية
│
├── 📁 venv/                      # البيئة الافتراضية (اختياري)
├── 📁 logs/                      # السجلات (تُنشأ تلقائياً)
└── 📁 exports/                   # الملفات المصدّرة (تُنشأ تلقائياً)
```

---

## 🎯 دليل الاستخدام الأساسي

### 1️⃣ Dashboard - لوحة المعلومات

**الغرض:** نظرة سريعة على البيانات

**ماذا تفعل:**
- شاهد عدد السحوبات (يجب أن يظهر 249)
- انظر إلى الخريطة الحرارية (32×32)
- اكتشف الأرقام الأكثر تكراراً

**نصيحة:** هذا أول شيء يجب أن تشاهده!

### 2️⃣ المولد الذكي - توليد التذاكر

**الغرض:** إنشاء تذاكر يانصيب ذكية

**خطوات البدء:**
1. اختر **"متوازنة"** من قائمة الخوارزميات
2. عدد التذاكر: **3**
3. حجم التذكرة: **6** (الافتراضي)
4. اضغط **"🎰 توليد تذاكر"**

**ماذا سترى:**
- 3 تذاكر مع أرقامها
- تحليل لكل تذكرة (ساخنة/باردة/متوازنة)
- المجموع والمعدل

**تصدير:**
- اضغط **"📥 Excel"** لحفظ التذاكر
- أو **"📄 PDF"** لنسخة قابلة للطباعة

### 3️⃣ الفاحص التاريخي

**الغرض:** فحص تذكرة ضد السحوبات السابقة

**مثال عملي:**
1. أدخل: `3, 7, 12, 19, 25, 30`
2. اضغط **"🔍 فحص التذكرة"**

**ماذا سترى:**
- عدد المرات التي تطابقت فيها 3 أرقام
- عدد المرات التي تطابقت فيها 4 أرقام
- إلخ...

### 4️⃣ التحليلات المتقدمة

**للمستخدمين المتقدمين**

**تحليل زمني:**
- كيف تتوزع الأرقام شهرياً
- أنماط موسمية

**مصفوفة الارتباط:**
- أزواج الأرقام التي تظهر معاً
- ثلاثيات مترابطة

**تحليل الفجوات:**
- كم سحبة مرت منذ آخر ظهور لكل رقم
- أرقام "متأخرة"

### 5️⃣ حاسبة الاحتمالات

**الغرض:** معرفة احتمالات الفوز

**استخدام بسيط:**
1. أدخل أرقام تذكرة
2. شاهد احتمالية كل فئة (3، 4، 5، 6 تطابقات)

**المحاكي:**
- اختر عدد السحوبات (مثلاً 10,000)
- اضغط "▶️ تشغيل المحاكاة"
- شاهد النتائج الإحصائية

---

## 💡 نصائح ذهبية للمبتدئين

### ✅ افعل:

1. **ابدأ بالـ Dashboard**
   - تعرّف على البيانات أولاً
   - لا تقفز مباشرة للتوليد

2. **جرب خوارزميات مختلفة**
   - "متوازنة" للتوازن بين كل شيء
   - "عشوائية ذكية" لمزيد من الحظ
   - "التنوع الأقصى" لتذاكر متباعدة

3. **استخدم الفاحص**
   - افحص تذاكرك القديمة
   - تعلم من الأنماط

4. **احفظ عملك**
   - صدّر التذاكر إلى Excel
   - احتفظ بنسخة من البيانات

5. **راقب الأداء**
   - استخدم "أدوات التصحيح"
   - امسح الذاكرة عند البطء

### ❌ لا تفعل:

1. **لا تنسى رفع البيانات**
   - التطبيق يحتاج ملف 249.xlsx
   - بدونه لن يعمل شيء

2. **لا تتوقع الفوز المضمون**
   - هذا تطبيق تحليلي، ليس سحرياً
   - اليانصيب دائماً عشوائي

3. **لا تستخدم قيود شديدة**
   - في التوليد المخصص
   - قد يفشل التوليد

4. **لا تشغل عدة نسخ**
   - نسخة واحدة من التطبيق كافية
   - لتجنب مشاكل المنافذ

5. **لا تتجاهل التحذيرات**
   - إذا ظهر تحذير، اقرأه
   - قد يحتوي على معلومات مهمة

---

## 🧪 اختبارات سريعة

### اختبار 1: هل Python مثبت؟

```bash
python --version
```

**يجب أن ترى:** `Python 3.8.x` أو أحدث

### اختبار 2: هل المكتبات مثبتة؟

```bash
python -c "import streamlit, pandas, numpy, plotly; print('✅ كل شيء مثبت')"
```

**يجب أن ترى:** `✅ كل شيء مثبت`

### اختبار 3: هل البيانات صحيحة؟

```bash
python -c "import pandas as pd; df = pd.read_excel('249.xlsx'); print(f'✅ {len(df)} سحبة محملة')"
```

**يجب أن ترى:** `✅ 249 سحبة محملة`

### اختبار 4: هل التطبيق يعمل؟

```bash
python run_app.py --test
```

**يجب أن ترى سلسلة من ✅**

---

## 📞 الحصول على المساعدة

### إذا فشل كل شيء:

1. **اجمع المعلومات:**
   ```bash
   python --version > system_info.txt
   pip list >> system_info.txt
   python diagnostic_test.py >> system_info.txt
   ```

2. **راجع السجلات:**
   ```bash
   cat logs/app_*.log
   ```

3. **أعد التثبيت من الصفر:**
   ```bash
   # احذف البيئة
   rm -rf venv/
   
   # أنشئ بيئة جديدة
   python -m venv venv
   source venv/bin/activate  # أو venv\Scripts\activate
   
   # ثبّت المكتبات
   pip install --upgrade pip
   pip install -r requirements_fixed.txt
   ```

### موارد إضافية:

- **TROUBLESHOOTING.md** - دليل شامل لحل المشاكل
- **README_DEBUG_TOOLS.md** - أدوات التصحيح المتقدمة
- **diagnostic_test.py --help** - مساعدة الأداة

---

## ✅ Checklist النهائي

قبل أن تسأل عن مساعدة، تأكد من:

- [ ] Python 3.8+ مثبت
- [ ] جميع المكتبات مثبتة (`pip list`)
- [ ] ملف 249.xlsx موجود
- [ ] التشخيص نجح (`python diagnostic_test.py`)
- [ ] المنفذ 8501 متاح
- [ ] المتصفح يعمل بشكل صحيح
- [ ] لا توجد برامج حماية تمنع الاتصال

---

## 🎓 دروس تعليمية سريعة

### الدرس 1: أول تذكرة (دقيقتان)

1. افتح التطبيق
2. ارفع 249.xlsx
3. اذهب لـ "المولد الذكي"
4. اضغط "توليد" (بالإعدادات الافتراضية)
5. انتهى! 🎉

### الدرس 2: فحص تذكرة (دقيقة واحدة)

1. اذهب لـ "الفاحص التاريخي"
2. أدخل: `1, 5, 10, 15, 20, 25`
3. اضغط "فحص"
4. شاهد النتائج

### الدرس 3: تصدير تذاكر (30 ثانية)

1. بعد التوليد
2. اضغط "📥 Excel"
3. افتح الملف (في مجلد التنزيلات)

---

## 🌟 الخطوات القادمة

بعد أن تتقن الأساسيات:

1. **جرب التوليد المخصص**
   - حدد نطاق مجموع
   - أضف أرقاماً إلزامية

2. **استكشف التحليلات المتقدمة**
   - مصفوفة الارتباط
   - تحليل الفجوات

3. **استخدم المحاكي**
   - شغّل 100,000 سحبة
   - احسب ROI

4. **خصص الواجهة**
   - غيّر الثيم (داكن/فاتح)
   - غيّر الألوان

---

<div align="center">

## 🎰 مستعد للبدء؟

```bash
python run_app.py --debug
```

**حظاً موفقاً! 🍀**

</div>

---

**آخر تحديث:** فبراير 2026  
**الإصدار:** 2.0.1 (محسّن)  
**صُنع بـ ❤️ للمبتدئين**
